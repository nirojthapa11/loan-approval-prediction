# Virtual environment
venv/
env/
.venv/

# Python
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Environment variables / secrets
.env
# Large / generated artifacts
# Raw and intermediate datasets are excluded (regenerate via notebooks 01-03,
# or download the raw CSV per README). Trained model artifacts in models/
# ARE committed intentionally -- they're small (a few KB) and required for
# app.py / the Flask API to run immediately after a clone, without forcing
# every user to first run src/train.py.
data/*.csv

# Logs
*.log
